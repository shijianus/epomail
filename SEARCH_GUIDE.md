# EpoCanvas Mail Search Guide

## 概述
搜索功能帮助您在海量邮件中快速定位信息。我们的搜索框不仅仅支持简单的关键字匹配，还支持通过前缀参数对搜索范围和显示效果进行进阶控制。

## 基本用法 (当前视图搜索)
默认情况下，当您在搜索框中输入任何文字（例如 `notion`），系统会 **仅在您当前所处的邮件分类下**（如：收件箱、发件箱、已归档等）进行搜索。

- **行为**：在当前文件夹中过滤邮件。
- **匹配字段**：邮件标题 (Subject)、发件人名称、寄收件人邮箱、邮件正文预览。
- **大小写**：默认不区分大小写（Case-insensitive）。
- **示例**：
  - 输入 `notion`：在当前所处的页面（例如收件箱）中查找包含 "notion" 或 "Notion" 的邮件。

---

## 进阶语法与参数

通过在搜索关键词前加入特定的参数标志（Parameter Flags），可以改变搜索的行为。

### 1. 全局搜索 (`global:`)
如果您希望打破当前分类的限制，在整个邮件账户（包含收件箱、已归档、已发送等所有邮件）中进行搜寻，请使用 `global:` 前缀。

- **语法**：`global:<您的关键字>`
- **行为**：自动跳转至「所有邮件」视图，并在整个邮箱中查找该关键词。
- **示例**：
  - 输入 `global:notion`：搜索您账户中**所有的**邮件，找出其中与 "notion" 相关的对话。

### 2. 状态与源过滤
您可以将以下参数添加到搜索框内进行精确查找：
- **`is:sent` 或 `from:me`**：在当前搜索范围中包含您自己发送的邮件。
- **`is:spam`**：强制在垃圾邮件文件夹中进行搜索。
- **`is:trash`**：强制在回收站中进行搜索。
- **`is:draft`**：自动跳转到草稿箱并在草稿箱内进行搜索。

### 3. 关闭高亮显示 (`hl:off`)
默认情况下，系统会自动用黄色高亮标出搜索结果中匹配的文字，以方便您快速排查。如果您觉得高亮颜色干扰阅读，可以通过 `hl:off` 参数将其关闭。

- **语法**：`hl:off <您的关键字>` 或者在搜索句中的任意位置加上 `hl:off`。
- **行为**：执行搜索，但不会在邮件列表中用黄色背景高亮关键字。
- **示例**：
  - 输入 `hl:off notion`：在当前视图搜索 "notion"，但不开启高亮。
  - 输入 `global:notion hl:off`：在全局范围内搜索 "notion"，同时关闭高亮显示。

---

## 高亮检视建议
1. **默认开启**：当您执行常规查询（如 `notion`）时，列表中发件人、邮件标题及正文预览中出现的 "notion" 字样会被 `<mark>` 标签包裹并呈现为醒目的颜色。这能直观地告诉您匹配命中在何处，帮助您判断这封邮件是否是您想找的。
2. **组合使用**：高亮功能与全局搜索完全兼容。当您使用 `global:notion` 捞出十几封相关邮件时，高亮能让您一眼看出究竟是正文提及了 Notion 还是标题包含了 Notion。
3. **关闭高亮的时机**：如果您正在做演示（Presentation）或截图，不希望满屏的高亮影响截图美观，只需在搜索框追加 `hl:off` 即可恢复纯净的阅读界面。
