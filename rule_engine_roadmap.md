# 邮件规则引擎与反垃圾分类管理升级计划 (Roadmap & Acceptance Targets)

本文档整理了系统规则引擎、反垃圾策略及分析面板的三个关键重构阶段。每阶段均明确了核心需求与验收指标，以确保符合业务逻辑且完全遵从 `AGENTS.md` 工作流规范。

---

## Phase 1: 规则优先级重构与自动化动作 (已完成待验收)

**核心目标**：打破原先“单纯打标签且规则平行”的简单逻辑，实现多级瀑布流控制（用户优先）和后续物理动作绑定。

*   **需求 1.1：三层优先级引擎**
    *   `Level 1 (绝对意志)`: 白名单/黑名单等带有最高优先级的自定义标签。
    *   `Level 2 (用户逻辑)`: 用户设定的常规邮件分类。
    *   `Level 3 (系统兜底)`: 默认黑盒逻辑（推销、订阅等）。
*   **需求 1.2：自动化动作支持 (Actions)**
    *   引擎从仅返回标签名称字符串，扩展为返回对象，支持 `skipInbox` (跳过收件箱)、`targetFolder` (指定目录：垃圾箱/归档)、`markAsRead` (自动标为已读)。

### 🟢 验收目标 (Acceptance Targets)
1.  [x] **代码无侵入破坏**：`mail-worker/src/email/rule-engine.js` 能够输出带有 `actions` 的结果。
2.  [x] **数据链路打通**：`mail-worker/src/email/email.js` 能够正确接收 `actions`，并将其翻译为数据库层面的 `isSpam`、`isDel` 与 `unread` 状态写入 D1 数据库。
3.  [x] **编译安全**：通过 `npm run test` 确保云端构建过程（vite 打包过程）未因后端依赖改变而崩溃。

---

## Phase 2: 反垃圾邮件 UX 与反馈循环 (待执行)

**核心目标**：实现类似 Gmail 的“垃圾邮件警示与一键纠错”能力，为系统的黑盒拦截提供用户级修正通道。

*   **需求 2.1：警示横幅 UI (Warning Banner)**
    *   在前端的邮件详情组件 (`MailDetail.vue` 或相近位置) 监听邮件状态。如果发现邮件在垃圾箱/推销分组内，需在标题下方注入一个显眼的警示区。
    *   文案需明确：“此邮件为何在垃圾邮件中？它与过去被识别为垃圾邮件的信息相似。”
*   **需求 2.2：不是垃圾邮件按钮与逻辑 (Report Not Spam)**
    *   提供“不是垃圾邮件”的交互按钮。
    *   **后端 API 响应**：点击后需调用新 API，完成两个动作：
        1. 邮件物理位置转移：从 spam 恢复回主要收件箱。
        2. 引擎反馈学习：将发件人地址推入用户的 **Level 1 全局信任名单** 中，确保后续此发件人将无视所有系统级推销拦截规则直达收件箱。

### 🟡 验收目标 (Acceptance Targets)
1.  [ ] **UI 一致性与截图验证**：必须启动本地前端，利用 Playwright 提供警示横幅的渲染截图，确保颜色、间距符合系统目前的 Outlook/现代风。
2.  [ ] **业务链路联调**：点击按钮后，前端需展现流畅的加载与归位动画。
3.  [ ] **白名单落库验证**：确认 API 成功更新了用户 `userRow.customLabels` 中的“信任名单”数据结构。

---

## Phase 3: 分类管理分析页与可视化 (待执行)

**核心目标**：在不增加庞大第三方库负担的前提下，为用户提供直观的系统拦截效果可视化大屏。

*   **需求 3.1：分析页 UI 入口与骨架**
    *   在原「分类管理」配置界面（`category-setting/index.vue`）新增一个“分析 (Analytics)”或“概览”页签。
*   **需求 3.2：纯净可视化面板 (Native Visualization)**
    *   使用纯 CSS (Grid/Flexbox) 构建数据大屏，不额外引入 Echarts 等重型图表库，贯彻高性能极简主义。
    *   **指标清单**：
        1. 累计处理总邮件量（可脱敏显示）。
        2. 推销/垃圾邮件拦截率或拦截总数条形图。
        3. 自定义规则活跃度排行榜 (Hit counts)。

### 🟡 验收目标 (Acceptance Targets)
1.  [ ] **前端渲染截图**：必须提供本地运行状态下，新版分类管理弹窗（包含 Analytics 面板）的真实截图。
2.  [ ] **极简代码实现**：确认 CSS 和组件设计是否足够精简。
3.  [ ] **部署检查**：走完所有的 Commit 与 `wrangler deploy` 流程，确保 CF 云端运行流畅。
